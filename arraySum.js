//complete the function below
//a function that takes in an array and returns the sum of all the elements in the array
//ex. arraySum([1,3]) would output 4

const arraySum = (array) => {

arraySum sum_of_elements(int *arr , int n)
{
    arraySum i=0,sum=0;
   
   for(i=0; i<n ; i++)
   {
       sum = sum + arr[i];
   }
   
   return sum;
}

// main function
arraySum main()
{
    arraySum total = 0;
    arraySum array[4] = {[1,2,3,2]};


    function arraySum() {
        var arraySum = 0;
        for (i = 0; i < 35; i++) {
            document.write("<td>" + i + "</td>")
        }

}
console.log(arraySum([1,2,3,2]);// should output 8
